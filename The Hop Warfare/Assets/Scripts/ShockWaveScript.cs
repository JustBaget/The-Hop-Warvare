using Unity.VisualScripting;
using UnityEngine;

public class ShockWaveScript : MonoBehaviour
{
    public float force;
    void Start()
    {
        
    }

    //void OnTriggerEnter(Collider other)
    //{
    //        other.GetComponent<Rigidbody>().AddForce(transform.position - other.transform.position * force, ForceMode.Impulse);
    //        Debug.Log("Shockwave happened!");
    //}
}
